describe('Ao chamar o controller listAll', () => {
  
  describe('quando existem produtos cadastrados', async () => {
    before(() => {
      
    });
  });
   
  describe('quando não existem produtos cadastrados', async () => {
    
  });

  describe('quando ocorre um erro no serviço', async () => {
    
  });
});
